//import db
// import queries
import { success, error } from '../../lib/components/globals';
import { generateToken } from '../../middleware/auth/jwt';
import { hashPassword, comparePasswords } from '../../middleware/auth/bcrypt';

